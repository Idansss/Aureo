{
  "name": "aureo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix app run dev",
    "build": "npm --prefix app run build",
    "start": "npm --prefix app run start"
  },
  "dependencies": {
    "next": "16.0.10"
  }
}
