# Aureo (Next.js) - environment variables
# Copy/paste into `app/.env.local` and replace the placeholder values.

# ---- Supabase (required) ----
# Supabase Dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL="https://YOUR_PROJECT_REF.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"

# Supabase service role key (server-side only).
# Required for admin/reporting flows that use `supabaseAdmin()` (e.g. `/admin/reports`).
SUPABASE_SERVICE_ROLE_KEY="YOUR_SUPABASE_SERVICE_ROLE_KEY"

# ---- Site URL (recommended) ----
# Used for auth redirects / email links.
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# ---- Cron protection (recommended for prod) ----
# Scheduler should call cron endpoints with:
# Authorization: Bearer <CRON_SECRET>
CRON_SECRET="REPLACE_WITH_LONG_RANDOM_SECRET"



